## You can recurse forever, because this language is actually built right

myRecursiveFunction Number:: arg1: Number
does
    arg1 = $arg1 + 1
    if $arg1 > 5
    does
        arg1 = 0
    end

    echo($arg1 >>> String + "\n")
    result: Number = $arg1
    
    myRecursiveFunction($arg1)
end

myRecursiveFunction(0)
exit(0) ## shouldn't happen