## You can recurse forever, because this language is actually built right

myRecursiveFunction Number 1:: Number
does
    arg[0] = arg[0] + 1
    if arg[0] > 5
    does
        arg[0] = 0
    end

    echo(arg[0] >>> String + "\n")
    result: Number = myRecursiveFunction(arg[0])
end

myRecursiveFunction(0)
exit(0) ## shouldn't happen