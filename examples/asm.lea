import stdlib

## For safety reasons, don't use anything starting with __lea
## Don't use also _main and main
## And any c stdantard library name, with or without the _ as prefix

asm("message: db 'Hello, World!', 10, 0")
asm("message_end:")
asm("push message")


asm("call printf", "linux")
asm("call _printf", "windows")

exit(0)